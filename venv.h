#include <stdbool.h>
#include <windows.h>
#include <string.h>
#include <stdbool.h>
#include <time.h>

int menuprincipal(char* escolha);

int containsLetters(const char* nome);

int containsNumbers(const char* idade);
  
void removequebralinha(char* str);

int Obras(int n);